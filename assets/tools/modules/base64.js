let initialized=false;
export function init(){if(initialized)return;initialized=true;const root=document.querySelector('#tool-base64'),input=root.querySelector('#b64Input'),output=root.querySelector('#b64Output');root.querySelector('[data-action="encode"]').addEventListener('click',()=>{try{output.textContent=btoa(unescape(encodeURIComponent(input.value)))}catch(error){output.textContent=`编码错误: ${error.message}`}});root.querySelector('[data-action="decode"]').addEventListener('click',()=>{try{output.textContent=decodeURIComponent(escape(atob(input.value.trim())))}catch(error){output.textContent=`解码错误: ${error.message}`}})}
